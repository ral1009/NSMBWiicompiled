schema_version: 1
workspace_root: ../..

project:
  id: nsmbw-d_en_bossNP
  display_name: NSMBW - d_en_bossNP.rel

memory:
  base: 0x80000000
  size: 0x01800000
  sda_base: 0x8042f980
  sda2_base: 0x80433360

inputs:
  dol:
    path: "NSMBW-Decomp/original/wiimj2d.dol"
  rel:
    path: "NSMBW-Decomp/original/d_en_bossNP.rel"
    load_address: 0x80B1C920

translation:
  allow_unsupported_instructions: false
  function_map:
    path: projects/nsmbw/function_map.txt

runtime:
  # Same native overrides the DOL translation uses: all four RELs are linked into one
  # executable, so a function overridden there must not be re-translated here.
  native_registration_root: projects/nsmbw/native
  native_abi_directories: [projects/nsmbw/native]

output:
  root: build/nsmbw-d_en_bossNP
  functions: functions
  runtime_config: RuntimeConfig.h
  data_initializer: data_sections_init.cpp
  base_manifest: base/base_manifest.json
